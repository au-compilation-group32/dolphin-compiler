// Negative test

void print_string (s:string) {
  output_string (s, get_stdout());
}

record Animal {
  name: string;
  legs: int;
}

record Animal {
  name: string;
  name: string;
  legs: int;
}

record Animal {
  name: string;
  name: int;
  legs: int;
}

record Animal2 {
  name: string;
  name: int;
  legs: int;
  legs: int;
  legs: int;
}

int main () {
  var x = 1;
  x.legs = 2;
  var cat: Animal = new Animal {name = "cat"; legs = 4;};
  cat.tail = 1;
  cat.legs = false;
  return 0;
}